package learn.streams.optionnal;

import java.util.Optional;

public class TestOptional {
    // Wrapper who can be empty
    // Cannot contain null
    // It's a final class
    // all constructors are private : we can't do new Optional()
    public static void main(String[] args) {
    }
}
