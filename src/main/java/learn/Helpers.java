package learn;

public class Helpers {

    //monButton.setOnAction(event -> System.out.println(event));
    // monButton.setOnAction(event -> System.out::println);
    //monButton.setOnAction((x, y) -> Math.pow(x, y));
    //monButton.setOnAction(Math.pow);

    //
    //
}

